八月十六号日报如下：
1.今天复习讲解了echarts.redis,MQ.echarts的话要导入echarts.js文件然后导入到jsp文件里面，然后复制对话框，和弹框 然后在创建两个数组 利用后台发过来的集合给数组对象赋值，之后导入图形option。redis的话需要导入依赖和配置文件，然后注入对象，调用相对的方法就可以。MQ的话也是要导入依赖和配置文件，然后需要注入对象，调用对应的方法就可以实现消息发送者，消息接收着需要在controller里面创建监听类之后创建和消息发送者一样的对象然后对消息进行接收。
2.自习课时间对今天讲的东西进行练习，还有今天需要写的的代码练习，对还不熟练的mq进行了加强练习。和一边抄写一边背诵面试题。